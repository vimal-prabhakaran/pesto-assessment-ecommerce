server:
  port: 8084

spring:
  application:
    name: order-manager-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vimal-prabhakaran/eureka-discovery-service-info.git
          clone-on-start: true